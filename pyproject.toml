[project]
name = "objdet_converter"
version = "1.1.0"
requires-python = ">= 3.7"
dependencies = [
    "numpy",
    "opencv-python"
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["*"]
exclude = [""]